# PROGRESS (append-only, honest)

Per entry: date · scope · works / stubbed / next.

- **{{DATE}} — setup.** works: drop-in `.claude/` harness (constitution, subagents, commands, hooks, state, scripts, git hooks). stubbed: none. next: `/start` → M1 ({{KEYSTONE}}).
